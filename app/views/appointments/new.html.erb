<%= form_with model: @appointment, local: true do |f| %>

  <% if @appointment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@appointment.errors.count, "error") %> prohibited this hospital from being saved:</h2>

      <ul>
        <% @appointment.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="container mt-5">
    <h1 class="mb-4">New appointment</h1>

    <%= render "form", appointment: @appointment %>

    <%= link_to "Back to appointments", appointments_path, class: 'btn btn-secondary' %>
    <%= link_to "Back to Dashboard", dashboard_path, class: "btn btn-dark" %>
  </div>
<% end %>