<div class="container mt-5">
  <h1 class="mb-4">Editing Doctor</h1>

  <%= form_with(model: @doctor, local: true) do |f| %>
    <% if @doctor.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@doctor.errors.count, "error") %> prohibited this hospital from being saved:</h2>

        <ul>
          <% @doctor.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <%= render "form", doctor: @doctor %>
  <% end %>

  <div class="d-flex">
    <%= link_to "Back to doctors", doctors_path, class: "btn btn-secondary" %>
    <%= link_to "Back to Dashboard", dashboard_path, class: "btn btn-dark" %>
  </div>
</div>